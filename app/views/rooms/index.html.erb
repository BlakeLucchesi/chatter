<h2>Chat Rooms</h2>
  
<div id="rooms">
  <% @rooms.each do |room| %>
    <div class="room">
      <h3><%= link_to room.title, room %> <%=raw '<small>(private)</small>' if room.private? %></h3>
    </div>
  <% end %>
</div>